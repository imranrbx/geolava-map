import React from 'react';
import useMapContext from '@/map/useMapContext';
import MapboxDraw from '@mapbox/mapbox-gl-draw';

const MapControls = () => {
  const { map, draw, setDraw } = useMapContext();
};

export default MapControls;
